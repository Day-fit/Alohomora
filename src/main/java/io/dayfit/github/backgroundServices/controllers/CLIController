@Controller
public class CLIController {
    private final CLICommandService cliCommandService;

    public CLIController(CLICommandService cliCommandService) {
        this.cliCommandService = cliCommandService;
    }

    @GetMapping("/cli")
    public String cli(@RequestParam command) {
        return cliCommandService.executeCommand(command);
    }
}